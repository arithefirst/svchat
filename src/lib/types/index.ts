export interface TypeMessage {
  message: string;
  imageSrc: string;
  user: string;
  uid: string;
  timestamp: Date;
}

export interface TypeFullMessage {
  channel: string;
  message: string;
  imageSrc: string;
  user: string;
  uid: string;
  timestamp: Date;
}
