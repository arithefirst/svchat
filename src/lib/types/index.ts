export interface TypeMessage {
  message: string;
  imageSrc: string;
  user: string;
}

export interface TypeFullMessage {
  channel: string;
  message: string;
  imageSrc: string;
  user: string;
}
