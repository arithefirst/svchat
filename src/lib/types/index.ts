export interface TypeMessage {
  message: string;
  imageSrc: string;
  user: string;
  timestamp: Date;
}

export interface TypeFullMessage {
  channel: string;
  message: string;
  imageSrc: string;
  user: string;
  timestamp: Date;
}
