export interface TypeMessage {
  message: string;
  imageSrc: string;
  user: string;
  timestamp: number;
}

export interface TypeFullMessage {
  channel: string;
  message: string;
  imageSrc: string;
  user: string;
  timestamp: number;
}
